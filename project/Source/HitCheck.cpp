#include "HitCheck.h"
#include <cmath>
#include <DxLib.h>

std::string HitCheck::CheckHitToPart(const Player& target, const VECTOR& attackPos, float attackRadius)
{
    // for (const SphereCollider& col : target.hitSpheres) { return; }

    return std::string();
}
